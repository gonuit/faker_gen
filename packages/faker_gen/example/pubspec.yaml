name: example
description: Example package demonstrating faker_gen usage with all FakeAs types.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  faker_annotation:
    path: ../../faker_annotation

dev_dependencies:
  build_runner: ^2.4.0
  faker_gen:
    path: ../
  lints: ^6.0.0
  test: ^1.25.6
